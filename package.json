{
  "name": "poc-cypress-e2e",
  "version": "1.0.0",
  "description": "POC - Cypres Interface and Service Tests",
  "main": "index.js",
  "scripts": {
    "start": "npx cypress open",
    "test": "npx cypress run --browser chrome",
    "stop": "npm stop",
    "cypress:run:dev": "CYPRESS_BASE=http://localhost:4200/ npx cypress run --browser chrome",
    "cypress:run:test": "CYPRESS_BASE=https://www.grocerycrud.com/demo/bootstrap_theme_v4/ npx cypress run --browser chrome",
    "cypress:run:hom": "CYPRESS_BASE=https://www.grocerycrud.com/demo/bootstrap_theme_v4/  npx cypress run --browser chrome"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arthurboeck/poc-cypress-e2e.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.5.0",
    "faker": "^4.1.0"
  }
}
